#pragma once

#include "BoardRepresentation.h"

const extern BoardSet kingMoves[64];
const extern BoardSet knightMoves[64];
// Negative diagonal, vertical, positive diagonal, horizontal (all from white's prespective)
const extern BoardSet pinRays[64][4];
